export default defineAppConfig({  
  ui: {
    primary: "dodgeroll-gold",
    gray: 'cool',
    button: {
      default: {
        size: "lg"
      },
      size: {
        lg: "text-base lg:text-lg"
      },
      padding: {
        lg: "py-2 px-4"
      }
    },
  }
})
